# Social Meadia App

This project has been built using the Vite+ReactJS app, a Major chunk of CSS with BootStrap, ReactIcons, Context API for state management, useReucer,uesEffect, adding and deleting posts through dummy JSON APIs, fetch Method is used, React-Router is also used for navigations, Some vanilla CSS is also applied......

  
